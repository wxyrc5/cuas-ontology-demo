# 事实口径与答辩边界

更新日期：2026-08-12（以本轮四个 Notebook 和 8 个 Streamlit 入口验收结果为准）

## 一句话项目定义

本项目是面向机场等重点场所的反无人平台语义决策与数字化试验原型：以本体统一“使命—场景—能力—装备—威胁—资产—人员—效果”，以 SPARQL 支持可追溯查询，以五通道贝叶斯反馈、全链路 OODA 和资源约束蜂群裁决辅助识别、预警、处置方案比较和效果复盘。

## 可以说

- 当前正式模型保持 8 个核心 Object Type、10 个核心 Link Type，并增加 3 个扩展类型、6 个扩展关系；浏览夹具包含 26 个核心数组实例和 45 条核心关系；
- TBox 393 triples，正向 ABox 260 triples，合并图 653 triples；
- 当前 TBox + 正向 ABox 经 HermiT 判为一致；同一个体同时属于互斥 Mission/Equipment 的负向对照被判为不一致；
- 正向 ABox 通过 SHACL，两个受控错误值被准确检出；
- 5 个 SPARQL 模板均直接查询正式 TBox + ABox，并分别返回 8/1/12/3/10 行；
- 四个规范 Notebook 本轮执行 43/43 个代码单元成功，Streamlit 8/8 个入口与页面通过；隔离端口真实启动健康检查为 200/ok；
- 五通道终态合成后验为 0.94594；持续达标收敛周期均值 23.805、P95 50；
- 同案例、同五通道、同观测预算下，贝叶斯 AUC=0.96789，固定规则 AUC=0.86881，ΔAUC=0.09909，配对 95% CI [0.09490, 0.10347]；
- 20 机 HPM 完整闭环时延均值 3.164 s、P90 3.670 s、P(小于5s)=0.9896；
- 20 机动态反馈闭环使命 Pk=0.7655，Wilson 95% CI [0.7596, 0.7713]；同资源静态覆盖表（无反馈）Pk=0.3007；
- 当前固定资源包在 Pk≥0.75 下的扫描前沿是 N=20；25 机 Pk=0.2903，30 机 Pk=0.0337，40/50/60/75/100 机为 0；
- 在同构并行资源包、共享融合航迹与批量授权的合成假设下，20/25/30/40/50/60/75/100 机达到协调感知点估计 Pk≥0.75 所需最少资源包分别为 1/2/2/2/3/3/4/5；对应动态 Pk 为 0.7593/0.9803/0.9767/0.8013/0.9627/0.8113/0.9153/0.8470；
- 在上述各规模最少达标资源包下，同资源静态冻结规则 Pk 分别为 0.2997/0.5303/0.2487/0.1353/0.1223/0.0617/0.0310/0.0137；静态规则达到 Pk≥0.75 分别需要 20 机 2 包、25 机 4 包，30–100 机在 6 包范围内均未达到；
- 若只按点估计 Pk≥0.95，20/25/30/40/50/60/75/100 机分别需要 2/2/2/3/3/4/5/6 包；100 机 6 包点估计为 0.9523，但 Wilson 95% 下界为 0.9441，因此不能表述为“95% 置信下保证 Pk≥0.95”；
- 100 机 6 包理想并行动态 Pk=0.9727，加入协调时延后为 0.9523；协调附加时延 P90=0.869 s，完整闭环小于 5 s 的概率为 0.9540。该协调模型是待标定工程假设，不是跨阵地实测；
- PKX/BRU/MUC 均有离线地图和三层非圆防御包络；20 机×60 帧连续性验收的最大逐帧位移为 0.298532 km、最大向外跳变为 0，二维/三维 20 个目标状态逐一同源；
- 当前 TBox+正向 ABox 为 653 triples；HermiT 正向一致、互斥类负向不一致，SHACL 正向通过且两个受控错误被检出。
- 本地 Effect→Action→Mission 压力夹具触发 8 条规则，其中 5 条自动写回会话状态、3 条进入人工授权队列，Action 含 L1/L2/L3、风险和一句话归因，导出的独立 RDF 图为 153 triples；Action JSON Schema 正向通过、缺少 Validation Function 的负向对照被拒绝，正式本体文件哈希不变。
- 本体条件化先验反事实在共享观测下得到名义/主雷达失效/射频光电备份分数 0.674/0.041/0.638；该值是合成敏感性分数，不是物理 Pk。
- 当前语义热插拔夹具加入 16 条合规声明后，原 HardKill 查询发现新装备，SHACL 通过，可执行代码文件改动为 0、查询不变、进程无需重启；该结论不外推到所有 SQL 架构。
- PKX/BRU/MUC 的 CRS84 输入分别归一化到 EPSG:32650/32631/32632；受控正常入口事件接受、超速且单源的异常事件隔离；规则门不能识别所有逼真假航迹。
- 20 目标 WTA 的容量、单目标唯一分配与禁配约束均通过；`ooda_20drone_latency_detail.csv` 有 20 行参数化毫秒流水账，逐行标注非实装遥测。

每次引用概率、时延或 AUC，都必须在同一页、同一张图或同一句中标明“参数化合成仿真”或“非现场测试”。

## 不可以说

- “国内首创”“国际领先”“已达到实战/部署要求”，除非有独立查新、鉴定或试验；
- “机场实测”“专家标注”“真实识别率”“实装拦截率”；
- 把 HermiT/SHACL 的当前版本结论扩大为“所有未来实例和 SWRL 规则均已证明正确”；
- 把参数化 OODA 时延表述为现场链路实测；
- 把合成使命 Pk 当作物理武器杀伤概率；
- 宣称固定资源能够对任意规模蜂群维持高成功率；
- 把“复制同构资源包”的规划实验说成已经部署多阵地，或把资源包数、合成行动成本说成采购报价；
- 宣称多资源包协调时延已由真实竞争链路、跨阵地台架、逐目标授权或现场电磁环境标定；
- 继续使用旧 AUC 0.9958/0.9922、旧固定规则 0.5682、旧 20 机 Pk 0.181、约 0.39 s 软件处理链或旧动画 8 s；
- 声称“SQL 必须修改 200 行、耗时两周”“本体只需一条三元组/五秒”，或把当前热插拔夹具推广成对所有关系数据库的普遍证明；
- 声称 DoDAF/UPDM 只是图片且不能机器执行，或项目已经通过其一致性认证；
- 声称当前 What-if/逆向 SPARQL 已完成 SCM 因果识别，或当前入口门能识别全部对抗性假航迹；
- 使用无法核实且编号错误匹配的 `GB/T 43585-2026《数字模型定义、分类与交互要求》`；
- 声称当前主模型默认采用 Jeffreys Beta(0.5,0.5) 先验；实际参数必须以 `bayes_params.json` 为准。
- 把二维/三维防御包络说成监管净空范围；
- 把本地 Action 原型说成商业平台部署、SWRL 在线执行、实时装备控制或已经批准的作战指令；
- 把北京大兴写成 PEK。正确代码是 PKX/ZBAD。

## 必须主动披露的缺口

1. OODA 分量、资源容量和单次作用 Pk 是可查询的工程假设，尚未由第三方或机场现场标定；
2. 当前固定资源在 25 机及以上不达标，失败结果必须保留；
3. 资源增配只复制独立并行动作容量，不提高单次作用 Pk，也不缩短基础 OODA；协调感知主口径加入随资源包数和目标规模增长的跨包同步、去冲突与指令编组时延，理想并行结果只作为上界；
4. 新增协调时延仍是参数化假设，尚未覆盖真实竞争链路吞吐、丢包、跨阵地时钟同步和物理射界冲突；逐目标人工授权、传感退化和 HPM 离线等压力工况仍会显著降低闭环使命成功率；
5. C2 断连仅完成本地降级 Action 契约，未部署多节点分布式一致性、自愈或恢复对账；
6. 失败数字线程是溯源与根因候选规则，SCM、do-算子和因果归因仍待干预/准实验数据；
7. Docker Compose 只提供可运行演示包装，Fuseki 尚未接入当前运行态主链，也不代表实时总线或真实装备接口。
8. SWRL 已作为设计态规则规范修正，运行态是独立的闭世界 SPARQL/确定性 Action 引擎；商业平台部署、SWRL 在线执行、真实专家标注一致性、真实传感器数据和机场现场试验不在本轮证据范围内；
9. 二维/三维轨迹、处置动作和设施位置均为参数化演示。

## 推荐答辩表述

“我们没有把软件仿真包装成装备实测。当前成果证明的是：语义热插拔能缩小当前夹具的变更面，8/10 核心加 3/6 扩展的形式模型可满足，设计态验证与运行态闭世界 Action 已分离；L1/L2/L3 授权、显式 WTA、完整 OODA 和五通道反馈共同形成 20 机任务闭环，并在同预算下优于固定规则。失败链可以复盘，健康条件可以改变先验，但都不冒充现场效能或已识别因果；进一步扩群只用于给出资源边界。”

## 唯一指标源

`05_Notebook与数据/验证输出/指标裁决.json` 是方案书、PPT、视频和网页的唯一数值裁决源。任何历史 PDF、PPT、视频、SRT、论文、README 或网页与其冲突时，以该文件为准。
